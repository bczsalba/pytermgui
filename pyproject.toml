[tool.pylint.messages_control]
disable = [
    "fixme",
        
    # If this is a problem, it should occur during runtime
    "not-callable"
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:"
]

omit = [
    "pytermgui/cmd.py"
]
